# Running the project

This project is a static HTML page. Replit Preview serves it through the configured
`Start application` workflow:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

Open Preview to view `index.html`.